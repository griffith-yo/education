import React from 'react'

const FooterInterval = () => {
  return (
    <div
      aria-live="polite"
      aria-atomic="true"
      style={{
        position: 'absolute',
        minHeight: '100px',
      }}
    />
  )
}

export default FooterInterval
